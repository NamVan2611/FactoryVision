# ============================================================
# Smart Factory Vision Inspection System - Backend Dependencies
# Python 3.12+
# ============================================================

# --- Web Framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17        # form/file uploads (image upload)

# --- Data Validation / Settings ---
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# --- Computer Vision ---
opencv-python==4.10.0.84
numpy==2.1.3
Pillow==11.0.0

# --- Database (ORM + Drivers) ---
SQLAlchemy==2.0.36
# SQL Server driver (production). Requires ODBC Driver 17/18 for SQL Server.
pyodbc==5.2.0
# SQLite is built into Python (development mode) - no package needed.

# --- Messaging / Communication ---
paho-mqtt==2.1.0                 # MQTT client (publish/subscribe)

# --- Security ---
python-jose[cryptography]==3.3.0 # JWT encode/decode
passlib[bcrypt]==1.7.4           # password hashing

# --- Reporting / Export ---
openpyxl==3.1.5                  # Excel export
reportlab==4.2.5                 # PDF export
pandas==2.2.3                    # CSV / data aggregation

# --- Utilities ---
websockets==14.1                 # WebSocket support for uvicorn

# --- Testing ---
pytest==8.3.4
httpx==0.28.1                    # async test client for FastAPI
